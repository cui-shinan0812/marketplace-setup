version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: marketplace-db
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: sportopia
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

